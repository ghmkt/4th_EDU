# Quest 제출 시 제목을 'Session03 이름'으로 해주세요. ex. Session03 김현세


# 1
# numpy, pandas를 활용하여, ‘sex ratio.csv’로부터 아래와 같은 형태를 가진 ‘new sex ratio.csv’를 반환하는 코드를 구현해주세요.
import pandas as pd
ori = pd.read_csv("C:/Users/lg01/Desktop/growthHackers/session_pdf/data/sex ratio.csv", index_col=0)
year = ori.columns
for i in range(len(ori.columns)):
    add = pd.DataFrame({"지역": [ori.iloc[0,i]]*len(year), "연도": year, "성비":ori.iloc[i]})
    df.append(add, ignore_index = True)
df.to_csv("C:/Users/lg01/Desktop/new_ratio.csv", encoding = 'EUC-KR')

# 2
# ‘province data.csv’로부터 지역별로 전 기간에 걸친 평균 gdp와 unemploymen를 구한 뒤, 이를 산포도로 표현하세요.
# 단, 평균 성비가 110 이상인 지역의 산포도는 파란색 X 모양의 점, 110 미만인 지역의 산포도는 빨간색 세모 모양의 점으로 표현하세요.
import pandas as pd
import numpy as np
from matplotlib import pyplot as plt
file = pd.read_csv("C:/Users/lg01/Desktop/growthHackers/session_pdf/data/province data.csv")
avg = file.groupby('id')['gdp','unemployment','sexratio'].mean()
up = avg[avg.sexratio>=110]
down = avg[avg.sexratio<110]    
plt.scatter(up['gdp'],up['unemployment'], color = 'blue', marker = 'x')
plt.scatter(down['gdp'],down['unemployment'], color = 'red', marker = '^')
plt.xlabel('GDP')
plt.ylabel('UNEMPLOYMENT')
